---
import Base from '../layouts/Base.astro';
import { SITE } from '../config';
---

<Base title={`Misc — ${SITE.name}`} description={`Miscellany from ${SITE.name}.`}>
  <h1>Misc</h1>
  <p>
    <!-- TODO: the grab-bag — talks, bookmarks, uses, colophon, whatever doesn't fit elsewhere -->
    Odds and ends that don't fit anywhere else.
  </p>
</Base>
