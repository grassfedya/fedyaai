---
import Base from '../layouts/Base.astro';
import { SITE } from '../config';
---

<Base title={`Not found — ${SITE.name}`} description="This page does not exist.">
  <h1>404 — Not found</h1>
  <p>Nothing lives at this address. Head back to the <a href="/">home page</a>.</p>
</Base>
